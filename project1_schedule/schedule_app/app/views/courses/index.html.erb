<h2>Listing courses</h2>

<table>
  <tr>
    <th>Name</th>
    <th>Classroom</th>
    <th>Maxsize</th>
    <th>Head Teacher</th>
    <th>Start date</th>
    <th>End date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @courses.each do |course| %>
  <tr>

    <td><%= link_to course.name %></td>
    <td><%= course.classroom.id %></td>
    <td><%= course.maxsize %></td>
    <!-- <td><%#= course.user_id.role("instructor")%></td> -->
    <td><%= course.start_date %></td>
    <td><%= course.end_date %></td>
    <td><%=  %></td>
   
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Course', new_course_path %>
